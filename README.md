# ExpenseTracker
This is a simple, self-contained Expense Tracker web app built with HTML, CSS, and JavaScript. It allows users to add expenses by name and amount, view a list with delete options, track a running total, and persist data in the browser's localStorage.
